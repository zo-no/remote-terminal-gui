# remote-terminal-gui

带 GUI 的远程 terminal，移动端友好，含快捷按钮。
基于 remote-terminal 基础版迭代。

## 依赖

```bash
brew install ttyd cloudflared  # macOS
# node 已内置
```

## 使用

```bash
./start.sh   # 启动，输出访问链接
./stop.sh    # 停止
```
